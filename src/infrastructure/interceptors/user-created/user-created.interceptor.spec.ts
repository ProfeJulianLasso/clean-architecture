import { UserCreatedInterceptor } from './user-created.interceptor';

describe('UserCreatedInterceptor', () => {
  it('should be defined', () => {
    expect(new UserCreatedInterceptor()).toBeDefined();
  });
});
