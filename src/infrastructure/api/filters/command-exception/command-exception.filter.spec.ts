import { CommandExceptionFilter } from './command-exception.filter';

describe('CommandExceptionFilter', () => {
  it('should be defined', () => {
    expect(new CommandExceptionFilter()).toBeDefined();
  });
});
